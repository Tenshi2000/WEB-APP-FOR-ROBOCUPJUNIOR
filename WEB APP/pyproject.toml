[tool.poetry]
name = "app"
version = "0.1.0"
description = ""
authors = ["Erik Mihálik"]
readme = "README.rst"

[tool.poetry.dependencies]
python = "^3.11"
Flask-AppBuilder = "^4.3.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
